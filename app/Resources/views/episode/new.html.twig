{% extends 'base.html.twig' %}

{% block body %}
   
<div class="row">
	<div class="col-xs-6 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-6 col-md-offset-1 col-lg-4 col-lg-offset-1">
		<ol class="breadcrumb">
	  		<li><a id="fil" href="{{ path('homepage') }}">Accueil</a></li>
	  		<li><a id="fil" href="{{ path('saison') }}">Episodes</a></li>
	  		<li class="active" >Ajout Episodes</li>
		</ol>
	</div>
</div>

<div class="row">
	 <div id="formcolor" class="col-xs-6 col-xs-offset-3">
        <h1 class="text-center">Ajout Episodes</h1>
        	{{ form_start(form) }}
        <div>
           	{{ form_label(form.nomEpisode) }}
            {{ form_widget(form.nomEpisode, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div>
            {{ form_widget(form.episodePoster) }}
        </div>
        <div>
            {{ form_label(form.numeroEpisode) }}
            {{ form_widget(form.numeroEpisode, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div>
            {{ form_label(form.resumeEpisode) }}
            {{ form_widget(form.resumeEpisode, {'attr': {'class': 'form-control'}}) }}
        </div>
            <a class="col-xs-1 col-xs-offset-10" href="{{ path('saison')}}"><button id="valider" class="btn btn-success"  type="submit" id="_submit" name="_submit" value="{{ 'Valider'}}">Valider</button></a>
        {{ form_end(form) }}

    </div>
</div>

{% endblock %}