{% extends 'base.html.twig' %}

{% block body %}

<div class="row">
	<div class="col-xs-6 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-6 col-md-offset-1 col-lg-4 col-lg-offset-1">
		<ol class="breadcrumb">
	  		<li><a id="fil" href="{{ path('homepage') }}">Accueil</a></li>
	  		<li><a id="fil" href="{{ path('séries') }}">Séries</a></li>
	  		<li class="active" >Ajout Séries</li>
		</ol>
	</div>
</div>
<div class="row">
	<div class="col-xs-8 text-justify col-xs-offset-4">
		<p> L'ajout de la série sera soumis à une modération. </p> 
		<p> Si elle correspond aux critères elle sera validée sous peu par un Admin. </p>
	</div>
</div>
<div class="row">
	 <div id="formcolor" class="col-xs-6 col-xs-offset-3">
        <h1 class="text-center">Ajouter une série</h1>
        {{ form_start(form) }}
        <div>
           	{{ form_label(form.nomSerie) }}
            {{ form_widget(form.nomSerie, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div>
            {{ form_widget(form.seriePoster) }}
        </div>
        <div>
            {{ form_label(form.resumeSerie) }}
            {{ form_widget(form.resumeSerie, {'attr': {'class': 'form-control'}}) }}
        </div>
            <a class="col-xs-1 col-xs-offset-10" href="{{ path('séries')}}"><button id="valider" class="btn btn-success"  type="submit" id="_submit" name="_submit" value="{{ 'Valider'}}">Valider</button></a>
        {{ form_end(form) }}
    </div>
</div>
	

{% endblock %}


