{% extends 'base.html.twig' %}

{% block body %}
    <h1>Series</h1>

    <table>
        <tbody>
            {{ form_start(update_form) }}
                {{ form_widget(update_form) }}
                <a class="col-xs-1 col-xs-offset-2" href="{{ path('séries')}}"><button id="connexion" class="btn btn-default"  type="submit" id="_submit" name="_submit" value="{{ 'Valider'}}" />Valider</button></a>
            {{ form_end(update_form) }}
        </tbody>
    </table>

    <ul>
        <a class="col-xs-1 col-xs-offset-4" href="{{ path('séries')}}"><button id="connexion" class="btn btn-default"  type="submit" id="_submit" name="_submit" value="{{ 'Valider'}}" />Revenir à la liste</button></a>
    </ul>
{% endblock %}
                