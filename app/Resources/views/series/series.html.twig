{% extends '::base.html.twig' %}


{% block body %}
<div class="row">
	<div class="col-xs-offset-8">
		<form class="navbar-form navbar-left" role="Search">
	        <div class="form-group">
	          <input type="text" class="form-control" placeholder="Rechercher">
	        </div>
	        <button type="submit" class="btn btn-default">Valider</button>
	      </form>
	</div>
</div>
<div class="row">
<a class="col-xs-1 col-xs-offset-10" href="{{ path('serie_new')}}"><button id="connexion" class="btn btn-default"  type="submit" id="_submit" name="_submit" value="{{ 'Ajouter'}}" />Ajouter</button></a>
	{% for serie in series %}
	<div class="col-xs-offset-1">
		<div class="col-xs-6 col-sm-3 col-md-3 col-lg-2">
		   	<figure>
		   		<a id="series" href="{{ path('sériesone', { 'id' : serie.id})}}"><figcaption>{{ serie.nomSerie }}</figcaption></a>
		   		<a id="series" href="{{ path('sériesone', { 'id' : serie.id})}}"><img id="imgserie" class="img-responsive" src="{{ serie.posterSerie }}" alt="poster"></a>
		   	</figure>
		</div>
	</div>
	{% endfor %}
</div>
{% endblock %}