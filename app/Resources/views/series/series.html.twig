{% extends '::base.html.twig' %}


{% block body %}
<div class="row">
	<div class="col-xs-12 col-xs-offset-4">
	   		{% for serie in series %}
	   			<a id="series" href="{{ path('sériesone', { 'id' : serie.id})}}"><h3>{{ serie.nomSerie }}</h3></a>
	   			<img id="imgserie" src="{{ serie.posterSerie }}" alt="poster">
	   		{% endfor %}
	</div>	
</div>
{% endblock %}